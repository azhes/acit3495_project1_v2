# acit3495_project1_v2

run the following docker commands:

docker build -f Dockerfile.web -t videostreaming .

docker build -f Dockerfile.auth -t authentication .

docker-compose -f docker-compose.yml up
